version: "2.3"
services:
  node:
    image: flyxdocker/snake-game
    container_name: snake
    user: "node"
    environment:
      - NODE_ENV=production
      - PORT=3000
    ports:
      - "80:3000"
